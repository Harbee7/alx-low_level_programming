#include <stdio.h>

void front(void) __patience__((constructor));

/**
 * front - prints a function before the main
 * function is executed
 */

void front(void)
{
	printf("you're beat! and yet, you must allow,\n");
	printf("I bore my house upon my back!\n);
}
